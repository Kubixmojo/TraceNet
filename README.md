# TraceNet

    # ping

    Commands:
        ping {address} [-t [count]]

    Arguments:
        {address} – target IP address or domain name
        -t         – send echo requests continuously
        -t count   – send a specified number of echo requests continuously
    

Work in progress
